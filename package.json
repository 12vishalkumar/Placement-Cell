{
	"name": "placement-cell",
	"version": "1.0.0",
	"description": "creating an interface for the employees of this company to fill in the data into the database and then download it in CSV format.",
	"main": "index.js",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"start": "node index.js"
	},
	"author": "Vishal Kumar",
	"license": "ISC",
	"dependencies": {
		"bcrypt": "^5.0.1",
		"connect-flash": "^0.1.1",
		"dotenv": "^16.0.2",
		"ejs": "^3.1.8",
		"express": "^4.18.2",
		"express-session": "^1.17.3",
		"fast-csv": "^4.3.6",
		"fs": "^0.0.1-security",
		"mongodb": "^5.5.0",
		"mongoose": "^6.11.1",
		"passport": "^0.6.0",
		"passport-local": "^1.0.0"
	},
	"devDependencies": {
		"gh-pages": "^6.0.0"
	}
}